# SDK compatibility harness output (see compat/README.md)
/compat-results-*.json

# Local task-tracking notes, not published
/backlog/

# Binaries built by `mise run build`
/bin/

# API key generated for the local Docker Compose cluster
/.cluster/

# SBOMs generated by `mise run sbom`; published per release, not committed.
sbom/
dist/
